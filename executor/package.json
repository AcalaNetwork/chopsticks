{
	"name": "@acala-network/chopsticks-executor",
	"description": "Chopsticks executor",
	"version": "0.7.4-0",
	"license": "Apache-2.0",
	"repository": {
		"type": "git",
		"url": "https://github.com/AcalaNetwork/chopsticks"
	},
	"scripts": {
		"clean": "rm -rf web node",
		"build": "wasm-pack build --target web --out-dir web; wasm-pack build --target nodejs --out-dir node"
	},
	"files": [
		"web/chopsticks_*",
		"node/chopsticks_*"
	],
	"main": "node/chopsticks_executor.js",
	"types": "web/chopsticks_executor.d.ts",
	"module": "web/chopsticks_executor.js",
	"stableVersion": "0.7.3"
}
